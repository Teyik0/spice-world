{
	"name": "@spice-world/entrypoint",
	"version": "0.1.0",
	"private": true,
	"module": "src/index.ts",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"dev": "bun run --bun --watch --inspect --title '@spice-world/server' src/index.ts",
		"build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile dist/server src/index.ts",
		"start": "./dist/server"
	},
	"dependencies": {
		"@spice-world/server": "workspace:*"
	}
}
