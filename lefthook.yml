pre-commit:
  parallel: true
  jobs:
    - name: files format
      run: bun run format
      glob:
        - "*.js"
        - "*.jsx"
        - "*.ts"
        - "*.tsx"
        - "*.json"
        - "*.jsonc"
        - "*.css"
      stage_fixed: true

    - name: typescript check (monorepo)
      run: bun run tsc --build
      glob: "*.{ts,tsx}"
      stage_fixed: true

pre-push:
  parallel: true
  jobs:
    - name: Server test
      run: bun run test
      root: apps/server
      glob: "*.{ts,tsx}"
      stage_fixed: true

commit-msg:
  commands:
    lint-commit-message:
      run: bunx commitlint --edit {1}
